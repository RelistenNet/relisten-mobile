# Ledger: Auth Session User Service Client

No implementation experiment has been claimed. This workstream remains backlog until local API config and Development-only server auth are ready.

When promoted, preregister the first experiment with timestamp, responsible agent, start commit, branch/worktree, mutable surface, validator, expected deliverable, expected artifacts, and linked ExecPlan if needed.
