# Ledger: CarPlay And Cast Playlist Identity

No implementation experiment has been claimed. This workstream remains backlog until Queue V2 runtime integration exists.

When promoted, preregister the first experiment before editing Cast or CarPlay queue code.
