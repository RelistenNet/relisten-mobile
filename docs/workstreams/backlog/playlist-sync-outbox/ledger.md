# Ledger: Playlist Sync Outbox

No implementation experiment has been claimed. This workstream remains backlog until scoped Realm, auth, and playlist server endpoints are ready.

When promoted, preregister the first experiment before adding sync runner or outbox code.
